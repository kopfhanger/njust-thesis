= 总结与展望

#import "../njust-thesis/lib.typ": th, compact-table

本章对全文的研究思路、示例模型和工程化边界进行总结。由于前文使用的是演示性数据，本章不把示例结果写成真实科学结论，而是展示结论章节中常见的归纳、限制和后续工作组织方式。

== 主要结论

=== 理论贡献

本文将喷射状态和时间状态放入统一的概率动力学框架，通过状态变量、观测方程和转移概率描述两个子系统的关联。第 2 章给出了物理量、单位和参数表的写法；第 3 章给出了定义、定理、证明和分段模型的组织方式；第 4 章说明如何将结构图与数据图放进连续论述；第 5 章则把算法步骤、异常分支和模块接口联系起来。

=== 方法与验证

验证过程应同时回答三个问题：模型是否能解释观测趋势，参数变化是否会导致合理的响应，以及异常数据是否会改变结论。对这三个问题，单独一张拟合图是不够的，还需要结合误差指标、参数表、残差分析和边界条件说明。真实论文接入后，可以保留本模板中的标签和交叉引用方式，仅替换数据和解释文字。

#figure(
  compact-table(table(
    columns: (1fr, 2fr, 1.2fr),
    align: (center + horizon, center + horizon, center + horizon),
    stroke: none,
    table.hline(stroke: 1pt),
    table.header(
      th[分析层次], th[需要回答的问题], th[推荐证据],
      table.hline(stroke: 0.5pt),
    ),
    [模型], [变量和边界条件是否定义清楚？], [公式与参数表],
    [数据], [观测趋势是否被模型解释？], [曲线与误差指标],
    [工程], [异常状态是否可追溯？], [算法与流程图],
    table.hline(stroke: 1pt),
  )),
  caption: [结论章节的证据组织方式],
) <tab:conclusions>

=== 结论的量化表达

结论部分可以把主要结果写成可核对的量化形式，而不是只给文字判断。下式把总体偏差分解为格式误差、数据误差和模型误差三部分，便于逐项说明哪一部分是本文改进的主要来源。

#math.equation(
  $ underbrace(norm(hat(bold(y)) - bold(y))_"总偏差", "观测量") <=
    underbrace(C_1 h^p, "格式") + underbrace(C_2 delta, "数据") + underbrace(C_3 epsilon, "模型") $,
) <eq:conclusion-decomposition>

当结论涉及多个指标时，应给出指标之间的权衡关系。下式用加权形式表示综合评价，权重需要说明确定依据：

#math.equation(
  $ J = w_1 "RMSE" + w_2 "MAE" + w_3 (1 - R^2), quad
    sum_(i=1)^3 w_i = 1, quad w_i >= 0 $,
) <eq:conclusion-score>

下表把研究问题、结论和证据类型对应起来，避免出现没有证据支撑的结论。真实论文中应逐行替换为本文的实际结果与图表编号。

#figure(
  compact-table(table(
    columns: (1.4fr, 1.9fr, 1.1fr),
    align: (center + horizon, center + horizon, center + horizon),
    stroke: none,
    table.hline(stroke: 1pt),
    table.header(
      th[研究问题], th[结论], th[证据类型],
      table.hline(stroke: 0.5pt),
    ),
    [状态估计的偏差来源], [格式误差随网格加密下降], [收敛性数值实验],
    [参数辨识的可信度], [区间估计覆盖率接近名义值], [重采样实验],
    [工程可部署性], [计算量与存储需求可控], [复杂度分析],
    table.hline(stroke: 1pt),
  )),
  caption: [研究问题、结论与证据类型的对应关系],
) <tab:conclusions>

=== 应用意义

在食品加工场景中，概率响应模型可以帮助研究者描述喷射效率随压力、黏度和温度变化的趋势；在时空导航场景中，状态转移模型可以帮助研究者表达预测区间和异常状态。上述表述仍然需要真实实验和工程验证支持，不应从模板示例直接外推。

== 局限性

当前示例模型使用了简化的独立误差假设，且没有引入真实装置的传感器延迟、材料非均匀性和边界扰动。图表也只展示了有限的数据维度，没有覆盖长时间序列、缺失观测和模型失配等情况。因此，正式论文应把这些限制写入方法章节和结论章节，而不是只在展望中笼统提及。

== 未来工作

=== 后续研究方向

未来工作可以围绕四个方向展开：第一，增加真实实验规模并进行跨设备验证；第二，使用相关噪声模型处理时间序列中的记忆效应；第三，改进参数辨识和不确定性传播方法；第四，将模型输出接入可复现的控制流程。每个方向都应给出明确的输入、评价指标和停止条件。

=== 模板使用建议

接入真实学位论文时，建议先复制本章的章节结构，再逐步替换正文、公式、图表和参考文献。CeTZ/Fletcher 只用于结构示意图，Lilaq 只用于数据图，定理和算法包均在需要的位置局部使用。不要把示例章节中的数据、作者信息或包级配置直接带入真实论文。

== 本章小结

本章总结了示例论文的模型、证据和限制，并说明了模板扩展的职责边界。六章正文共享同一套页面、段落、公式和图表规则，扩展包只生成内容对象，不接管模板的整体排版。
